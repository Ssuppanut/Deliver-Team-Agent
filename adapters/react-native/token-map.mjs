/** React Native token map — JS token object from tokens-rn.ts (no CSS vars). */
export const mapToken = (name) => `tokens.${name}`;
export const STYLE_PROP = {
  background: 'backgroundColor',
  color: 'color',
  padding: 'padding',
  radius: 'borderRadius',
  gap: 'gap',
  border: 'borderColor',
  fontSize: 'fontSize',
};
